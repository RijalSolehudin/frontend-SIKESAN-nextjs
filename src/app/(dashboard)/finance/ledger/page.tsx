import { LedgerView } from '@/features/finance/components/LedgerView';

export const metadata = {
  title: 'Buku Besar (Ledger) | SIKESAN',
};

export default function LedgerPage() {
  return <LedgerView />;
}
