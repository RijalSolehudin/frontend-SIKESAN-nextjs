import { DormitoryView } from '@/features/master-data/components/DormitoryView';

export const metadata = {
  title: 'Data Asrama | SIKESAN',
};

export default function DormitoriesPage() {
  return <DormitoryView />;
}
