import { StudentView } from '@/features/master-data/components/StudentView';

export const metadata = {
  title: 'Data Santri | SIKESAN',
};

export default function StudentsPage() {
  return <StudentView />;
}
